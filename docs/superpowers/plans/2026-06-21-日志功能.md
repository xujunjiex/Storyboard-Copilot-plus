# 日志功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Storyboard Copilot Plus 添加结构化、可配置、可调试的前端日志体系，与现有后端 `tracing` 体系统一时间线持久化。

**Architecture:** 自建 `src/lib/logger/` 前端模块（命名空间 + 级别 + 字段 + ring buffer），通过 Tauri command `append_frontend_log` 把日志送到后端，复用 `tracing` 文件 appender 写入 `storyboard.log`。dev-only 浮动面板订阅 ring buffer。`localStorage` 存运行时配置。

**Tech Stack:** TypeScript + React + Zustand + Vitest；Rust + Tauri 2 + `tracing` + `tracing-subscriber`（已存在，不增依赖）。

---

## 全局约束

- **零新依赖**：前端不增 npm 包；后端不增 crate。`@xyflow/react`（已用）、`zustand`（已用）、`uuid`（已用）外不引入其他库。
- **Tauri 2 命令注册**：`src-tauri/src/lib.rs` 的 `tauri::generate_handler![...]` 列表必须包含每个新命令。
- **TypeScript strict 模式**：保持项目现有的 `tsconfig.json` 严格性，新增文件不允许 `any`（除 `LogFields`）。
- **Tauri IPC 探测**：用 `typeof window.__TAURI_INTERNALS__ === 'undefined'` 判断是否在 Tauri 中，非 Tauri 模式下跳过 IPC。
- **ESLint 规则**：`no-console` 改为 `warn` 级别，不强制失败；保留必要的 `console.error`（React/Vite 内部需要）。
- **现有日志命令迁移**：项目已有 `image::log_frontend_debug`（写 `frontend_debug.log` 独立文件）和 `system::write_debug_log`（写 debug 文件）。本计划实施后，统一改用新 `logger`，**删除这两个旧命令**（最后一次替换完成时移除）。
- **i18n 规则**（CLAUDE.md 第 12 节）：UI 文案走 i18n key，不写死中英文字面量。LogPanel 中的"暂无日志"、"打开日志目录"等需要在 `zh.json` / `en.json` 增加对应 key。
- **CLAUDE.md 第 5.5 节**：现有调试策略保留（`info!` 后端、`console.info/error` 前端），但**优先用新 logger**。

---

## 文件结构

### 新建

**前端**（`src/lib/logger/`）：
- `index.ts` — 聚合导出 `logger`、`getLogger`、`setLogConfig`、`getLogConfig`、`resetLogConfig`
- `types.ts` — `Level`、`LogFields`、`LogEntry`、`LogConfig`、`Logger` 接口
- `levels.ts` — 级别常量与比较函数
- `namespace.ts` — `Error().stack` 解析调用文件，转换为点分路径
- `serialize.ts` — 字段安全序列化（循环引用、Error 实例、截断大对象）
- `config.ts` — `localStorage` 读写、合并默认值
- `ringBuffer.ts` — 容量 500 的循环覆盖缓冲
- `transport.ts` — console + ring buffer + IPC 三个输出，批量节流
- `store.ts` — zustand store：ring buffer 引用、面板开关状态
- `useLogPanelHotkey.ts` — 全局快捷键 hook
- `LogPanel.tsx` — dev-only 浮动 UI
- `__tests__/levels.test.ts`
- `__tests__/namespace.test.ts`
- `__tests__/serialize.test.ts`
- `__tests__/config.test.ts`
- `__tests__/ringBuffer.test.ts`
- `__tests__/transport.test.ts`

**后端**（`src-tauri/src/`）：
- `session.rs` — `SessionId` 状态
- `commands/logging.rs` — `append_frontend_log` Tauri command
- `commands/cleanup.rs` — `cleanup_old_logs(log_dir, keep_days)` 工具函数

**i18n**：
- `src/i18n/locales/zh.json` — 加 `logger.*` 键
- `src/i18n/locales/en.json` — 加 `logger.*` 键

### 修改

- `src-tauri/src/lib.rs` — 注册 `SessionId`、`append_frontend_log`、`cleanup_old_logs`；启动时调用 `cleanup_old_logs`
- `src-tauri/src/commands/mod.rs` — 导出 `logging`、`cleanup` 模块
- `src-tauri/src/commands/image.rs` — 删除 `log_frontend_debug` 函数
- `src-tauri/src/commands/system.rs` — 删除 `write_debug_log` 函数
- `src/App.tsx` — dev-only 挂载 `<LogPanel />` + 调用 `useLogPanelHotkey`
- `src/components/SettingsDialog.tsx` — 新增 `logging` 分类 UI
- `src/features/settings/settingsEvents.ts` — `SettingsCategory` 加 `'logging'`
- 20 个含 `console.*` 的文件 — 批量替换
- `src/commands/image.ts` — 删除 `logFrontendDebug` 函数
- `src/commands/system.ts` — 删除 `writeDebugLog` 函数及调用方
- `.eslintrc` — 加 `no-console: warn`
- `src/i18n/locales/zh.json` + `en.json` — 加 logger 相关文案

### 不修改

- `src-tauri/Cargo.toml`（不增依赖）
- `package.json`（不增依赖）
- `tailwind.config.js`（LogPanel 用现有 token）

---

## Task 1: 后端 `SessionId` 状态

**Files:**
- Create: `src-tauri/src/session.rs`
- Modify: `src-tauri/src/lib.rs:1-16` (顶层 use + 注册)

**Interfaces:**
- Produces: `pub struct SessionId(pub String)` with `pub fn new() -> Self`
- Consumes: `uuid::Uuid::new_v4().to_string()` (uuid crate 已存在)

- [ ] **Step 1: 创建 `src-tauri/src/session.rs`**

```rust
pub struct SessionId(pub String);

impl SessionId {
    pub fn new() -> Self {
        Self(uuid::Uuid::new_v4().to_string())
    }
}

impl Default for SessionId {
    fn default() -> Self {
        Self::new()
    }
}
```

- [ ] **Step 2: 在 `src-tauri/src/lib.rs` 顶部加 `pub mod session;`**

Edit `src-tauri/src/lib.rs:1` 区域：

```rust
pub mod ai;
pub mod commands;
pub mod session;
```

- [ ] **Step 3: 在 `.setup(|app| ...)` 闭包内（`lib.rs:98` 附近）注册 `SessionId`**

在 `Ok(())` 之前、`app.handle().clone()` 那一行附近，加：

```rust
let session_id = session::SessionId::new();
info!("session_id = {}", session_id.0);
app.manage(session_id);
```

- [ ] **Step 4: 运行 `cargo check` 验证**

Run: `cd src-tauri && cargo check`
Expected: 编译通过，无 warning 报 `unused import`（如果不是首次，需要 `use` `session::SessionId`）。

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src-tauri/src/session.rs src-tauri/src/lib.rs
git commit -m "feat(logging): add SessionId state and register in run()"
```

---

## Task 2: 后端 `append_frontend_log` command

**Files:**
- Create: `src-tauri/src/commands/logging.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs:162-211` (invoke_handler 列表)

**Interfaces:**
- Produces: `pub async fn append_frontend_log(app: AppHandle, entry: FrontendLogEntry)` - 无返回值

- [ ] **Step 1: 创建 `src-tauri/src/commands/logging.rs`**

```rust
use serde::Deserialize;
use tauri::AppHandle;

use crate::session::SessionId;

#[derive(Deserialize)]
pub struct FrontendLogEntry {
    pub level: String,
    pub target: String,
    pub message: String,
    pub fields: Option<serde_json::Value>,
    pub ts_ms: i64,
}

#[tauri::command]
pub async fn append_frontend_log(app: AppHandle, entry: FrontendLogEntry) -> Result<(), String> {
    let session_id = app
        .state::<SessionId>()
        .0
        .clone();
    let fields = entry.fields.unwrap_or(serde_json::json!({}));
    let enriched = serde_json::json!({
        "session": session_id,
        "client_ts_ms": entry.ts_ms,
        "fields": fields,
    });

    match entry.level.as_str() {
        "debug" => tracing::debug!(target: "frontend", ns = %entry.target, data = %enriched, "{}", entry.message),
        "info"  => tracing::info!(target:  "frontend", ns = %entry.target, data = %enriched, "{}", entry.message),
        "warn"  => tracing::warn!(target:  "frontend", ns = %entry.target, data = %enriched, "{}", entry.message),
        "error" => tracing::error!(target: "frontend", ns = %entry.target, data = %enriched, "{}", entry.message),
        _ => return Err(format!("invalid level: {}", entry.level)),
    }
    Ok(())
}
```

- [ ] **Step 2: 在 `src-tauri/src/commands/mod.rs` 导出 logging**

修改 `src-tauri/src/commands/mod.rs`：

```rust
pub mod ai;
pub mod cleanup;
pub mod image;
pub mod logging;
pub mod media;
pub mod project_state;
pub mod system;
pub mod update;

pub use ai::*;
pub use cleanup::*;
pub use image::*;
pub use logging::*;
pub use media::*;
pub use project_state::*;
pub use system::*;
pub use update::*;
```

> `cleanup` 模块在 Task 3 创建。先在 mod.rs 中声明，但 `pub use cleanup::*;` 暂不写，等 Task 3 完成后补上（或现在写也 OK，编译会报"找不到模块"前先建好 Task 3 文件）。

- [ ] **Step 3: 在 `src-tauri/src/lib.rs` 的 `invoke_handler` 列表中注册**

在 `update::check_latest_release_tag,` 之后（`lib.rs:210` 附近）加：

```rust
            logging::append_frontend_log,
```

- [ ] **Step 4: 运行 `cargo check` 验证**

Run: `cd src-tauri && cargo check`
Expected: 编译通过，warning 不超过 1-2 个 unused 之类。

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src-tauri/src/commands/logging.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat(logging): add append_frontend_log Tauri command"
```

---

## Task 3: 后端 `cleanup_old_logs` 工具 + 启动清理

**Files:**
- Create: `src-tauri/src/commands/cleanup.rs`
- Modify: `src-tauri/src/commands/mod.rs` (cleanup 已在 Task 2 加，但 `pub use cleanup::*` 需要 cleanup.rs 存在)
- Modify: `src-tauri/src/lib.rs:43-64` (setup_logging 末尾)

**Interfaces:**
- Produces: `pub fn cleanup_old_logs(dir: &Path, keep_days: u64)` - 删除 N 天前的 `storyboard.log.*` 文件
- Consumes: 调用方传入日志目录路径

- [ ] **Step 1: 创建 `src-tauri/src/commands/cleanup.rs`**

```rust
use std::path::Path;
use std::time::{Duration, SystemTime};

const KEEP_DAYS: u64 = 14;
const LOG_PREFIX: &str = "storyboard.log.";

pub fn cleanup_old_logs(dir: &Path) {
    cleanup_old_logs_with_keep(dir, KEEP_DAYS);
}

fn cleanup_old_logs_with_keep(dir: &Path, keep_days: u64) {
    let cutoff = SystemTime::now()
        .checked_sub(Duration::from_secs(keep_days * 24 * 60 * 60))
        .unwrap_or(SystemTime::UNIX_EPOCH);

    let entries = match std::fs::read_dir(dir) {
        Ok(entries) => entries,
        Err(_) => return,
    };

    for entry in entries.flatten() {
        let path = entry.path();
        if !path.is_file() {
            continue;
        }
        let name = match path.file_name().and_then(|n| n.to_str()) {
            Some(n) => n,
            None => continue,
        };
        if !name.starts_with(LOG_PREFIX) {
            continue;
        }
        let modified = entry
            .metadata()
            .and_then(|m| m.modified())
            .unwrap_or(SystemTime::UNIX_EPOCH);
        if modified < cutoff {
            let _ = std::fs::remove_file(&path);
        }
    }
}
```

- [ ] **Step 2: 确认 `src-tauri/src/commands/mod.rs` 的 `pub use cleanup::*;`**

（Task 2 Step 2 已加，但 `cleanup` 模块的 `pub use` 需要这个文件存在。如果已存在则跳过此步。）

- [ ] **Step 3: 在 `lib.rs` 的 `setup_logging` 函数末尾加清理调用**

修改 `src-tauri/src/lib.rs:43-64`，在 `info!("Storyboard Copilot starting...");` 这一行后加：

```rust
    if let Some(log_dir) = resolve_log_dir() {
        commands::cleanup::cleanup_old_logs(&log_dir);
    }
```

- [ ] **Step 4: 运行 `cargo check` 验证**

Run: `cd src-tauri && cargo check`
Expected: 编译通过。

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src-tauri/src/commands/cleanup.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat(logging): cleanup old log files on startup (keep 14 days)"
```

---

## Task 4: 前端 `logger/types.ts` + `logger/levels.ts`

**Files:**
- Create: `src/lib/logger/types.ts`
- Create: `src/lib/logger/levels.ts`

- [ ] **Step 1: 创建 `src/lib/logger/types.ts`**

```typescript
export type Level = 'debug' | 'info' | 'warn' | 'error';

export type LogFields = Record<string, unknown>;

export interface Logger {
  debug(msg: string, fields?: LogFields): void;
  info(msg: string, fields?: LogFields): void;
  warn(msg: string, fields?: LogFields): void;
  error(msg: string, fields?: LogFields): void;
}

export interface LogConfig {
  level: Level;
  moduleLevels: Record<string, Level>;
  console: boolean;
  persist: boolean;
  consoleTimestamps: boolean;
}

export const DEFAULT_LOG_CONFIG: LogConfig = {
  level: 'debug',
  moduleLevels: {},
  console: true,
  persist: true,
  consoleTimestamps: false,
};

export interface LogEntry {
  id: string;
  ts: number;
  level: Level;
  target: string;
  message: string;
  fields: LogFields;
}
```

- [ ] **Step 2: 创建 `src/lib/logger/levels.ts`**

```typescript
import type { Level } from './types';

const LEVEL_ORDER: Record<Level, number> = {
  debug: 0,
  info: 1,
  warn: 2,
  error: 3,
};

export function levelValue(level: Level): number {
  return LEVEL_ORDER[level];
}

export function isLevelEnabled(target: Level, globalLevel: Level): boolean {
  return levelValue(target) >= levelValue(globalLevel);
}

export function resolveLevel(
  target: string,
  config: { level: Level; moduleLevels: Record<string, Level> },
): Level {
  // 最长前缀匹配
  const entries = Object.entries(config.moduleLevels);
  let bestMatch: { prefix: string; level: Level } | null = null;
  for (const [prefix, lvl] of entries) {
    if (target === prefix || target.startsWith(prefix + '.')) {
      if (!bestMatch || prefix.length > bestMatch.prefix.length) {
        bestMatch = { prefix, level: lvl };
      }
    }
  }
  return bestMatch?.level ?? config.level;
}
```

- [ ] **Step 3: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/types.ts src/lib/logger/levels.ts
git commit -m "feat(logging): add types and level resolution"
```

---

## Task 5: 前端 `logger/levels.test.ts` + `logger/namespace.ts`

**Files:**
- Create: `src/lib/logger/__tests__/levels.test.ts`
- Create: `src/lib/logger/namespace.ts`

- [ ] **Step 1: 写失败测试 `src/lib/logger/__tests__/levels.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { isLevelEnabled, levelValue, resolveLevel } from '../levels';
import type { LogConfig } from '../types';

describe('levelValue', () => {
  it('returns numeric order for each level', () => {
    expect(levelValue('debug')).toBe(0);
    expect(levelValue('info')).toBe(1);
    expect(levelValue('warn')).toBe(2);
    expect(levelValue('error')).toBe(3);
  });
});

describe('isLevelEnabled', () => {
  it('passes when target >= threshold', () => {
    expect(isLevelEnabled('warn', 'info')).toBe(true);
    expect(isLevelEnabled('info', 'info')).toBe(true);
  });

  it('rejects when target < threshold', () => {
    expect(isLevelEnabled('debug', 'info')).toBe(false);
  });
});

describe('resolveLevel', () => {
  const baseConfig: LogConfig = {
    level: 'info',
    moduleLevels: {},
    console: true,
    persist: true,
    consoleTimestamps: false,
  };

  it('returns global level when no override', () => {
    expect(resolveLevel('canvas.Canvas', baseConfig)).toBe('info');
  });

  it('matches exact prefix', () => {
    expect(resolveLevel('canvas', { ...baseConfig, moduleLevels: { canvas: 'debug' } })).toBe('debug');
  });

  it('matches dotted prefix', () => {
    expect(resolveLevel('canvas.Canvas', { ...baseConfig, moduleLevels: { canvas: 'debug' } })).toBe('debug');
  });

  it('uses longest prefix when multiple match', () => {
    expect(
      resolveLevel('canvas.Canvas', {
        ...baseConfig,
        moduleLevels: { canvas: 'debug', 'canvas.Canvas': 'error' },
      }),
    ).toBe('error');
  });

  it('returns global level when prefix does not match', () => {
    expect(
      resolveLevel('ai.providers', { ...baseConfig, moduleLevels: { canvas: 'debug' } }),
    ).toBe('info');
  });
});
```

- [ ] **Step 2: 跑测试验证通过（Task 4 已实现，测试应该过）**

Run: `cd D:/xjj20/Desktop/itv_2 && npx vitest run src/lib/logger/__tests__/levels.test.ts`
Expected: 8 tests passed.

- [ ] **Step 3: 创建 `src/lib/logger/namespace.ts`**

```typescript
const UNKNOWN = 'unknown';

export function resolveNamespace(depth: number = 3): string {
  const stack = new Error().stack;
  if (!stack) return UNKNOWN;

  const lines = stack.split('\n');
  // depth=0 是 resolveNamespace 自己；depth=1 是调用者；depth=2 是更上一级
  // 我们要的是调用 logger 的那一行；Logger 内部又会包一层，所以一般 depth=3 起步
  const line = lines[depth];
  if (!line) return UNKNOWN;

  const match = line.match(/at\s+(?:.*\()?(.+?):\d+:\d+\)?$/);
  if (!match) return UNKNOWN;

  return fileToNamespace(match[1]);
}

function fileToNamespace(filePath: string): string {
  // 去掉 query / hash (Vite 风格的 `?t=123`)
  const clean = filePath.split('?')[0].split('#')[0];
  // 找到 src/ 之后的部分；如果是 node_modules 内文件则直接保留
  const idx = clean.lastIndexOf('src/');
  const relative = idx >= 0 ? clean.slice(idx + 4) : clean.replace(/^.*[\\/]/, '');

  // 去掉扩展名
  const noExt = relative.replace(/\.(ts|tsx|js|jsx)$/, '');

  // 统一 / 和 \ 为 .，去掉 index 收尾
  return noExt
    .replace(/\\/g, '/')
    .replace(/\/+/g, '/')
    .replace(/\/index$/, '')
    .replace(/\//g, '.')
    .replace(/^\.+|\.+$/g, '');
}
```

- [ ] **Step 4: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/__tests__/levels.test.ts src/lib/logger/namespace.ts
git commit -m "feat(logging): add level tests and namespace resolver"
```

---

## Task 6: 前端 `logger/namespace.test.ts` + `logger/serialize.ts`

**Files:**
- Create: `src/lib/logger/__tests__/namespace.test.ts`
- Create: `src/lib/logger/serialize.ts`

- [ ] **Step 1: 写失败测试 `src/lib/logger/__tests__/namespace.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { resolveNamespace } from '../namespace';

describe('fileToNamespace via resolveNamespace', () => {
  it('converts file path to dot path', () => {
    // simulate stack: frame at "src/features/canvas/Canvas.tsx:123:4"
    const fakeStack = `Error
    at Object.<anonymous> (src/features/canvas/Canvas.tsx:123:4)
    at resolveNamespace (namespace.ts:1:1)
    at Object.debug (logger/index.ts:50:1)`;

    const err = new Error();
    err.stack = fakeStack;
    const originalError = global.Error;
    (global as any).Error = function () {
      return err;
    };
    try {
      const ns = resolveNamespace();
      expect(ns).toBe('features.canvas.Canvas');
    } finally {
      (global as any).Error = originalError;
    }
  });

  it('returns unknown when stack is unavailable', () => {
    const originalError = global.Error;
    (global as any).Error = function () {
      return { stack: undefined };
    };
    try {
      expect(resolveNamespace()).toBe('unknown');
    } finally {
      (global as any).Error = originalError;
    }
  });
});
```

- [ ] **Step 2: 创建 `src/lib/logger/serialize.ts`**

```typescript
import type { LogFields } from './types';

const MAX_FIELD_BYTES = 10 * 1024;
const CIRCULAR_MARKER = '<circular>';
const TRUNCATED_MARKER = '<truncated>';

export function serializeFields(fields: LogFields | undefined): LogFields {
  if (!fields) return {};
  const seen = new WeakSet<object>();
  const result: LogFields = {};

  for (const [key, value] of Object.entries(fields)) {
    result[key] = serializeValue(value, seen);
  }

  return truncateIfLarge(result);
}

function serializeValue(value: unknown, seen: WeakSet<object>): unknown {
  if (value === null || value === undefined) return value;
  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') {
    return value;
  }
  if (typeof value === 'bigint') return value.toString();
  if (typeof value === 'function') return `[Function: ${value.name || 'anonymous'}]`;
  if (value instanceof Error) {
    return {
      __error: true,
      name: value.name,
      message: value.message,
      stack: value.stack,
    };
  }
  if (value instanceof Date) return value.toISOString();
  if (value instanceof Map) {
    return {
      __map: true,
      entries: Array.from(value.entries()).map(([k, v]) => [k, serializeValue(v, seen)]),
    };
  }
  if (value instanceof Set) {
    return {
      __set: true,
      values: Array.from(value.values()).map((v) => serializeValue(v, seen)),
    };
  }
  if (typeof value === 'object') {
    const obj = value as object;
    if (seen.has(obj)) return CIRCULAR_MARKER;
    seen.add(obj);
    const out: LogFields = {};
    for (const [k, v] of Object.entries(obj as Record<string, unknown>)) {
      out[k] = serializeValue(v, seen);
    }
    return out;
  }
  return String(value);
}

function truncateIfLarge(obj: LogFields): LogFields {
  let serialized: string;
  try {
    serialized = JSON.stringify(obj);
  } catch {
    return { __serialization_error: true };
  }
  if (serialized.length <= MAX_FIELD_BYTES) return obj;
  return {
    __truncated: true,
    preview: serialized.slice(0, MAX_FIELD_BYTES),
    original_bytes: serialized.length,
  };
}
```

- [ ] **Step 3: 跑 namespace 测试 + 新增 serialize 测试（先写 serialize 测试）**

新增 `src/lib/logger/__tests__/serialize.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { serializeFields } from '../serialize';

describe('serializeFields', () => {
  it('passes through primitives', () => {
    expect(serializeFields({ a: 1, b: 'x', c: true, d: null })).toEqual({
      a: 1, b: 'x', c: true, d: null,
    });
  });

  it('marks circular references', () => {
    const a: any = { x: 1 };
    a.self = a;
    const result = serializeFields({ a });
    expect((result.a as any).self).toBe('<circular>');
  });

  it('extracts Error info', () => {
    const err = new Error('boom');
    const result = serializeFields({ err });
    expect((result.err as any).__error).toBe(true);
    expect((result.err as any).message).toBe('boom');
  });

  it('truncates large fields', () => {
    const big = 'x'.repeat(20_000);
    const result = serializeFields({ big });
    expect((result.big as any).__truncated).toBe(true);
  });

  it('handles undefined input', () => {
    expect(serializeFields(undefined)).toEqual({});
  });
});
```

- [ ] **Step 4: 跑测试验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx vitest run src/lib/logger/__tests__/`
Expected: 全部测试通过。

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/__tests__/namespace.test.ts src/lib/logger/__tests__/serialize.test.ts src/lib/logger/serialize.ts
git commit -m "feat(logging): add namespace and serialize tests"
```

---

## Task 7: 前端 `logger/ringBuffer.ts` + `logger/config.ts`

**Files:**
- Create: `src/lib/logger/ringBuffer.ts`
- Create: `src/lib/logger/config.ts`
- Create: `src/lib/logger/__tests__/ringBuffer.test.ts`
- Create: `src/lib/logger/__tests__/config.test.ts`

- [ ] **Step 1: 写失败测试 `src/lib/logger/__tests__/ringBuffer.test.ts`**

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { RingBuffer } from '../ringBuffer';
import type { LogEntry } from '../types';

function makeEntry(i: number): LogEntry {
  return {
    id: String(i),
    ts: i,
    level: 'info',
    target: 't',
    message: 'm' + i,
    fields: {},
  };
}

describe('RingBuffer', () => {
  let buf: RingBuffer;
  beforeEach(() => {
    buf = new RingBuffer(3);
  });

  it('stores entries up to capacity', () => {
    buf.push(makeEntry(1));
    buf.push(makeEntry(2));
    buf.push(makeEntry(3));
    expect(buf.size()).toBe(3);
    expect(buf.entries().map((e) => e.message)).toEqual(['m1', 'm2', 'm3']);
  });

  it('overwrites oldest when over capacity', () => {
    buf.push(makeEntry(1));
    buf.push(makeEntry(2));
    buf.push(makeEntry(3));
    buf.push(makeEntry(4));
    expect(buf.size()).toBe(3);
    expect(buf.entries().map((e) => e.message)).toEqual(['m2', 'm3', 'm4']);
  });

  it('clear empties the buffer', () => {
    buf.push(makeEntry(1));
    buf.clear();
    expect(buf.size()).toBe(0);
  });
});
```

- [ ] **Step 2: 创建 `src/lib/logger/ringBuffer.ts`**

```typescript
import type { LogEntry } from './types';

export class RingBuffer {
  private items: LogEntry[] = [];
  constructor(private readonly capacity: number) {}

  push(entry: LogEntry): void {
    this.items.push(entry);
    if (this.items.length > this.capacity) {
      this.items.shift();
    }
  }

  entries(): readonly LogEntry[] {
    return this.items;
  }

  size(): number {
    return this.items.length;
  }

  clear(): void {
    this.items = [];
  }
}
```

- [ ] **Step 3: 写失败测试 `src/lib/logger/__tests__/config.test.ts`**

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { loadConfig, saveConfig, STORAGE_KEY } from '../config';
import { DEFAULT_LOG_CONFIG } from '../types';

describe('config', () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it('returns defaults when storage is empty', () => {
    expect(loadConfig()).toEqual(DEFAULT_LOG_CONFIG);
  });

  it('round-trips a saved config', () => {
    saveConfig({ level: 'warn', moduleLevels: { canvas: 'debug' } });
    const cfg = loadConfig();
    expect(cfg.level).toBe('warn');
    expect(cfg.moduleLevels).toEqual({ canvas: 'debug' });
    // other fields keep defaults
    expect(cfg.console).toBe(DEFAULT_LOG_CONFIG.console);
  });

  it('uses storage key itv.log.config', () => {
    expect(STORAGE_KEY).toBe('itv.log.config');
  });
});
```

- [ ] **Step 4: 创建 `src/lib/logger/config.ts`**

```typescript
import { DEFAULT_LOG_CONFIG, type LogConfig } from './types';

export const STORAGE_KEY = 'itv.log.config';

let cached: LogConfig | null = null;

export function loadConfig(): LogConfig {
  if (cached) return cached;
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) {
      cached = { ...DEFAULT_LOG_CONFIG };
      return cached;
    }
    const parsed = JSON.parse(raw) as Partial<LogConfig>;
    cached = { ...DEFAULT_LOG_CONFIG, ...parsed };
    return cached;
  } catch {
    cached = { ...DEFAULT_LOG_CONFIG };
    return cached;
  }
}

export function saveConfig(partial: Partial<LogConfig>): LogConfig {
  const current = loadConfig();
  const next = { ...current, ...partial };
  cached = next;
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
  } catch {
    // ignore quota or privacy mode
  }
  return next;
}

export function resetConfig(): void {
  cached = { ...DEFAULT_LOG_CONFIG };
  try {
    localStorage.removeItem(STORAGE_KEY);
  } catch {
    // ignore
  }
}
```

- [ ] **Step 5: 跑测试验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx vitest run src/lib/logger/__tests__/`
Expected: 全部通过（levels/namespace/serialize/ringBuffer/config 5 个文件）。

- [ ] **Step 6: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/ringBuffer.ts src/lib/logger/config.ts src/lib/logger/__tests__/ringBuffer.test.ts src/lib/logger/__tests__/config.test.ts
git commit -m "feat(logging): add ring buffer and config storage with tests"
```

---

## Task 8: 前端 `logger/transport.ts` + store

**Files:**
- Create: `src/lib/logger/transport.ts`
- Create: `src/lib/logger/store.ts`
- Create: `src/lib/logger/__tests__/transport.test.ts`

- [ ] **Step 1: 写失败测试 `src/lib/logger/__tests__/transport.test.ts`**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

// Mock Tauri internals
const invokeMock = vi.fn();
(global as any).window = (global as any).window || {};
(global as any).window.__TAURI_INTERNALS__ = { invoke: invokeMock };

import { Transport } from '../transport';
import type { LogEntry } from '../types';
import { DEFAULT_LOG_CONFIG } from '../types';

function makeEntry(level: LogEntry['level'] = 'info'): LogEntry {
  return {
    id: '1',
    ts: 0,
    level,
    target: 't',
    message: 'm',
    fields: {},
  };
}

describe('Transport', () => {
  let transport: Transport;
  let consoleLog: ReturnType<typeof vi.spyOn>;
  let consoleError: ReturnType<typeof vi.spyOn>;
  let consoleWarn: ReturnType<typeof vi.spyOn>;

  beforeEach(() => {
    invokeMock.mockReset();
    consoleLog = vi.spyOn(console, 'info').mockImplementation(() => {});
    consoleError = vi.spyOn(console, 'error').mockImplementation(() => {});
    consoleWarn = vi.spyOn(console, 'warn').mockImplementation(() => {});
    transport = new Transport();
  });

  afterEach(() => {
    consoleLog.mockRestore();
    consoleError.mockRestore();
    consoleWarn.mockRestore();
    transport.flushSync();
  });

  it('writes to console when config.console=true', () => {
    transport.send(makeEntry(), { ...DEFAULT_LOG_CONFIG });
    expect(consoleLog).toHaveBeenCalled();
  });

  it('skips console when config.console=false', () => {
    transport.send(makeEntry(), { ...DEFAULT_LOG_CONFIG, console: false });
    expect(consoleLog).not.toHaveBeenCalled();
  });

  it('invokes Tauri when persist=true', () => {
    transport.send(makeEntry(), { ...DEFAULT_LOG_CONFIG });
    expect(invokeMock).toHaveBeenCalledWith('append_frontend_log', expect.any(Object));
  });

  it('skips Tauri when persist=false', () => {
    transport.send(makeEntry(), { ...DEFAULT_LOG_CONFIG, persist: false });
    expect(invokeMock).not.toHaveBeenCalled();
  });

  it('routes error to console.error', () => {
    transport.send(makeEntry('error'), { ...DEFAULT_LOG_CONFIG });
    expect(consoleError).toHaveBeenCalled();
  });

  it('routes warn to console.warn', () => {
    transport.send(makeEntry('warn'), { ...DEFAULT_LOG_CONFIG });
    expect(consoleWarn).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 创建 `src/lib/logger/store.ts`**

```typescript
import { create } from 'zustand';
import type { LogEntry, Level } from './types';
import { RingBuffer } from './ringBuffer';

const RING_CAPACITY = 500;

interface LogStore {
  buffer: RingBuffer;
  open: boolean;
  minimized: boolean;
  levelFilter: Set<Level>;
  namespaceFilter: string | null;
  textQuery: string;
  subscribe(listener: () => void): () => void;
  toggleOpen(): void;
  setOpen(v: boolean): void;
  setMinimized(v: boolean): void;
  toggleLevelFilter(level: Level): void;
  setNamespaceFilter(ns: string | null): void;
  setTextQuery(q: string): void;
  appendEntry(entry: LogEntry): void;
  clearBuffer(): void;
  snapshot(): readonly LogEntry[];
}

let listeners = new Set<() => void>();
function notify() {
  for (const l of listeners) l();
}

export const useLogStore = create<LogStore>((set, get) => ({
  buffer: new RingBuffer(RING_CAPACITY),
  open: false,
  minimized: false,
  levelFilter: new Set<Level>(['debug', 'info', 'warn', 'error']),
  namespaceFilter: null,
  textQuery: '',
  subscribe(listener) {
    listeners.add(listener);
    return () => listeners.delete(listener);
  },
  toggleOpen() {
    set((s) => ({ open: !s.open, minimized: false }));
    notify();
  },
  setOpen(v) {
    set({ open: v, minimized: false });
    notify();
  },
  setMinimized(v) {
    set({ minimized: v });
    notify();
  },
  toggleLevelFilter(level) {
    const cur = new Set(get().levelFilter);
    if (cur.has(level)) cur.delete(level);
    else cur.add(level);
    set({ levelFilter: cur });
    notify();
  },
  setNamespaceFilter(ns) {
    set({ namespaceFilter: ns });
    notify();
  },
  setTextQuery(q) {
    set({ textQuery: q });
    notify();
  },
  appendEntry(entry) {
    get().buffer.push(entry);
    notify();
  },
  clearBuffer() {
    get().buffer.clear();
    notify();
  },
  snapshot() {
    return get().buffer.entries();
  },
}));
```

- [ ] **Step 3: 创建 `src/lib/logger/transport.ts`**

```typescript
import { invoke } from '@tauri-apps/api/core';
import type { LogConfig, LogEntry } from './types';
import { isLevelEnabled, resolveLevel } from './levels';
import { serializeFields } from './serialize';
import { useLogStore } from './store';

const FLUSH_INTERVAL_MS = 200;

export class Transport {
  private queue: LogEntry[] = [];
  private timer: ReturnType<typeof setTimeout> | null = null;

  send(entry: LogEntry, config: LogConfig): void {
    const effective = resolveLevel(entry.target, config);
    if (!isLevelEnabled(entry.level, effective)) return;

    if (config.console) this.writeConsole(entry, config);
    useLogStore.getState().appendEntry(entry);

    if (config.persist && this.isTauri()) {
      this.queue.push(entry);
      this.scheduleFlush();
    }
  }

  flushSync(): void {
    if (this.queue.length === 0) return;
    const batch = this.queue.splice(0);
    void this.flush(batch);
  }

  private isTauri(): boolean {
    return typeof (window as any).__TAURI_INTERNALS__ !== 'undefined';
  }

  private writeConsole(entry: LogEntry, config: LogConfig): void {
    const tag = `[${entry.target}]`;
    const text = config.consoleTimestamps
      ? `${new Date(entry.ts).toISOString()} ${tag} ${entry.message}`
      : `${tag} ${entry.message}`;
    const args: unknown[] = [text];
    if (Object.keys(entry.fields).length > 0) {
      args.push(entry.fields);
    }
    const fn =
      entry.level === 'error' ? console.error :
      entry.level === 'warn' ? console.warn :
      console.info;
    fn(...args);
  }

  private scheduleFlush(): void {
    if (this.timer) return;
    this.timer = setTimeout(() => {
      this.timer = null;
      this.flushSync();
    }, FLUSH_INTERVAL_MS);
  }

  private async flush(batch: LogEntry[]): Promise<void> {
    for (const entry of batch) {
      try {
        await invoke('append_frontend_log', {
          entry: {
            level: entry.level,
            target: entry.target,
            message: entry.message,
            fields: serializeFields(entry.fields),
            ts_ms: entry.ts,
          },
        });
      } catch (err) {
        // 静默：后端不可用不阻塞前端
      }
    }
  }
}

export const globalTransport = new Transport();
```

- [ ] **Step 4: 跑测试验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx vitest run src/lib/logger/__tests__/transport.test.ts`
Expected: 6 tests passed.

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/transport.ts src/lib/logger/store.ts src/lib/logger/__tests__/transport.test.ts
git commit -m "feat(logging): add transport, zustand store and tests"
```

---

## Task 9: 前端 `logger/index.ts`（聚合 + 顶层 API）

**Files:**
- Create: `src/lib/logger/index.ts`

- [ ] **Step 1: 创建 `src/lib/logger/index.ts`**

```typescript
import { v4 as uuidv4 } from 'uuid';

import type { Level, LogFields, Logger, LogConfig } from './types';
import { loadConfig, saveConfig, resetConfig } from './config';
import { isLevelEnabled, resolveLevel } from './levels';
import { resolveNamespace } from './namespace';
import { serializeFields } from './serialize';
import { globalTransport } from './transport';

function emit(level: Level, target: string, msg: string, fields?: LogFields): void {
  const config = loadConfig();
  const effective = resolveLevel(target, config);
  if (!isLevelEnabled(level, effective)) return;

  const message =
    fields && fields.err instanceof Error
      ? `${msg} | ${(fields.err as Error).stack ?? (fields.err as Error).message}`
      : msg;

  const entry = {
    id: uuidv4(),
    ts: Date.now(),
    level,
    target,
    message,
    fields: serializeFields(fields),
  };
  globalTransport.send(entry, config);
}

function makeLogger(target: string): Logger {
  return {
    debug: (msg, f) => emit('debug', target, msg, f),
    info: (msg, f) => emit('info', target, msg, f),
    warn: (msg, f) => emit('warn', target, msg, f),
    error: (msg, f) => emit('error', target, msg, f),
  };
}

export const logger: Logger = makeLogger(resolveNamespace(2));

export function getLogger(ns: string): Logger {
  return makeLogger(ns);
}

export function setLogConfig(cfg: Partial<LogConfig>): void {
  saveConfig(cfg);
}

export function getLogConfig(): LogConfig {
  return loadConfig();
}

export function resetLogConfig(): void {
  resetConfig();
}

export { useLogStore } from './store';
export type { LogEntry, LogConfig, Level, LogFields, Logger } from './types';
```

- [ ] **Step 2: 跑 tsc 验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 3: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/index.ts
git commit -m "feat(logging): aggregate exports and top-level logger API"
```

---

## Task 10: 前端 `useLogPanelHotkey.ts` + `LogPanel.tsx`

**Files:**
- Create: `src/lib/logger/useLogPanelHotkey.ts`
- Create: `src/lib/logger/LogPanel.tsx`
- Create: `src/i18n/locales/zh.json` (修改)
- Create: `src/i18n/locales/en.json` (修改)

- [ ] **Step 1: 在 i18n 加文案**

修改 `src/i18n/locales/zh.json`，找一个合适位置（如 `common` 下）增加：

```json
{
  "logger": {
    "panel": {
      "title": "日志面板",
      "empty": "暂无日志",
      "openFolder": "打开日志目录",
      "copyAll": "复制最近 100 条",
      "clear": "清空",
      "export": "导出",
      "search": "搜索…",
      "noResults": "没有匹配的日志",
      "close": "关闭",
      "minimize": "最小化",
      "level": "级别"
    }
  }
}
```

修改 `src/i18n/locales/en.json`，对应：

```json
{
  "logger": {
    "panel": {
      "title": "Log Panel",
      "empty": "No logs yet",
      "openFolder": "Open log directory",
      "copyAll": "Copy last 100",
      "clear": "Clear",
      "export": "Export",
      "search": "Search…",
      "noResults": "No matching logs",
      "close": "Close",
      "minimize": "Minimize",
      "level": "Level"
    }
  }
}
```

> 注：实际项目 i18n 文件结构以现有为准，这里假设是平铺在 `common` 旁。具体路径通过读 `src/i18n/index.ts` 确认。

- [ ] **Step 2: 创建 `src/lib/logger/useLogPanelHotkey.ts`**

```typescript
import { useEffect } from 'react';
import { useLogStore } from './store';

function isEditableTarget(target: EventTarget | null): boolean {
  if (!(target instanceof HTMLElement)) return false;
  const tag = target.tagName;
  if (tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT') return true;
  if (target.isContentEditable) return true;
  return false;
}

export function useLogPanelHotkey(): void {
  const toggleOpen = useLogStore((s) => s.toggleOpen);

  useEffect(() => {
    function handler(e: KeyboardEvent) {
      if (isEditableTarget(e.target)) return;
      const isMod = e.ctrlKey || e.metaKey;
      if (isMod && e.shiftKey && (e.key === 'L' || e.key === 'l')) {
        e.preventDefault();
        toggleOpen();
      }
    }
    window.addEventListener('keydown', handler);
    return () => window.removeEventListener('keydown', handler);
  }, [toggleOpen]);
}
```

- [ ] **Step 3: 创建 `src/lib/logger/LogPanel.tsx`**

```typescript
import { useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { useLogStore } from './store';
import type { Level, LogEntry } from './types';

const ALL_LEVELS: Level[] = ['debug', 'info', 'warn', 'error'];
const LEVEL_COLOR: Record<Level, string> = {
  debug: 'text-gray-400',
  info: 'text-blue-400',
  warn: 'text-yellow-400',
  error: 'text-red-400',
};

export function LogPanel(): JSX.Element | null {
  const { t } = useTranslation();
  const open = useLogStore((s) => s.open);
  const minimized = useLogStore((s) => s.minimized);
  const levelFilter = useLogStore((s) => s.levelFilter);
  const namespaceFilter = useLogStore((s) => s.namespaceFilter);
  const textQuery = useLogStore((s) => s.textQuery);
  const buffer = useLogStore((s) => s.buffer);
  const toggleLevelFilter = useLogStore((s) => s.toggleLevelFilter);
  const setNamespaceFilter = useLogStore((s) => s.setNamespaceFilter);
  const setTextQuery = useLogStore((s) => s.setTextQuery);
  const setMinimized = useLogStore((s) => s.setMinimized);
  const setOpen = useLogStore((s) => s.setOpen);
  const clearBuffer = useLogStore((s) => s.clearBuffer);

  // 订阅 ring buffer 变化强制 re-render（zustand 自身不会 deep watch）
  const [, force] = useState(0);
  useEffect(() => useLogStore.getState().subscribe(() => force((n) => n + 1)), []);

  if (!open) return null;

  const allEntries = buffer.entries();
  const namespaces = Array.from(new Set(allEntries.map((e) => e.target))).sort();

  const visible = allEntries.filter((e) => {
    if (!levelFilter.has(e.level)) return false;
    if (namespaceFilter && e.target !== namespaceFilter) return false;
    if (textQuery) {
      const q = textQuery.toLowerCase();
      if (!e.message.toLowerCase().includes(q) &&
          !JSON.stringify(e.fields).toLowerCase().includes(q)) {
        return false;
      }
    }
    return true;
  }).slice().reverse();

  return (
    <div
      data-testid="log-panel"
      className="fixed bottom-4 right-4 z-[9999] w-[480px] bg-zinc-900 text-zinc-100 rounded-lg shadow-2xl flex flex-col"
      style={{ height: minimized ? 40 : 320 }}
    >
      <div className="flex items-center gap-2 px-3 py-2 border-b border-zinc-700">
        <span className="text-sm font-semibold flex-1">{t('logger.panel.title')}</span>
        <button
          onClick={() => setMinimized(!minimized)}
          className="text-xs px-2 py-1 hover:bg-zinc-800 rounded"
          aria-label={t('logger.panel.minimize')}
        >
          {minimized ? '▢' : '—'}
        </button>
        <button
          onClick={() => setOpen(false)}
          className="text-xs px-2 py-1 hover:bg-zinc-800 rounded"
          aria-label={t('logger.panel.close')}
        >
          ×
        </button>
      </div>

      {!minimized && (
        <>
          <div className="flex items-center gap-1 px-2 py-1 border-b border-zinc-700 text-xs">
            {ALL_LEVELS.map((lv) => (
              <button
                key={lv}
                onClick={() => toggleLevelFilter(lv)}
                className={`px-2 py-0.5 rounded ${
                  levelFilter.has(lv) ? LEVEL_COLOR[lv] + ' bg-zinc-800' : 'text-zinc-600 bg-zinc-900'
                }`}
              >
                {lv}
              </button>
            ))}
            <input
              type="text"
              value={textQuery}
              onChange={(e) => setTextQuery(e.target.value)}
              placeholder={t('logger.panel.search')}
              className="ml-auto px-2 py-0.5 bg-zinc-800 rounded text-xs w-32"
            />
            <button
              onClick={clearBuffer}
              className="px-2 py-0.5 hover:bg-zinc-800 rounded text-xs"
            >
              {t('logger.panel.clear')}
            </button>
          </div>

          <div className="flex flex-wrap gap-1 px-2 py-1 border-b border-zinc-700 text-[10px] max-h-12 overflow-y-auto">
            <button
              onClick={() => setNamespaceFilter(null)}
              className={`px-1.5 py-0.5 rounded ${namespaceFilter === null ? 'bg-zinc-700' : 'hover:bg-zinc-800'}`}
            >
              all
            </button>
            {namespaces.map((ns) => (
              <button
                key={ns}
                onClick={() => setNamespaceFilter(ns)}
                className={`px-1.5 py-0.5 rounded ${
                  namespaceFilter === ns ? 'bg-zinc-700' : 'hover:bg-zinc-800'
                }`}
              >
                {ns}
              </button>
            ))}
          </div>

          <div className="flex-1 overflow-y-auto font-mono text-[11px] leading-tight">
            {visible.length === 0 ? (
              <div className="text-zinc-500 text-center py-8">
                {allEntries.length === 0 ? t('logger.panel.empty') : t('logger.panel.noResults')}
              </div>
            ) : (
              visible.map((entry) => <LogLine key={entry.id} entry={entry} />)
            )}
          </div>
        </>
      )}
    </div>
  );
}

function LogLine({ entry }: { entry: LogEntry }) {
  return (
    <div className="px-2 py-1 border-b border-zinc-800 hover:bg-zinc-800">
      <span className={`font-bold ${LEVEL_COLOR[entry.level]}`}>
        {entry.level.toUpperCase().padEnd(5)}
      </span>
      <span className="text-zinc-500 ml-2">{entry.target}</span>
      <span className="ml-2">{entry.message}</span>
      {Object.keys(entry.fields).length > 0 && (
        <details className="ml-2 mt-1">
          <summary className="text-zinc-500 cursor-pointer">fields</summary>
          <pre className="text-zinc-400 ml-4 whitespace-pre-wrap break-all">
            {JSON.stringify(entry.fields, null, 2)}
          </pre>
        </details>
      )}
    </div>
  );
}
```

- [ ] **Step 4: 跑 tsc 验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx tsc --noEmit`
Expected: 无错误。如果 i18n 路径不同，可能需要调整 `useTranslation` 引入。

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/lib/logger/useLogPanelHotkey.ts src/lib/logger/LogPanel.tsx src/i18n/locales/zh.json src/i18n/locales/en.json
git commit -m "feat(logging): add LogPanel UI and global hotkey hook"
```

---

## Task 11: 在 App.tsx 挂载 LogPanel（dev-only）

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 在 `App.tsx` 顶部加 import**

```typescript
import { LogPanel, useLogPanelHotkey } from '@/lib/logger';
```

> 注：实际项目是否使用 `@/lib/logger` 路径别名以 `tsconfig.json` 为准。

- [ ] **Step 2: 在 `App` 组件函数体内加**

```typescript
function App() {
  useLogPanelHotkey();
  // ... 现有代码
  return (
    <>
      {/* 现有 JSX */}
      {import.meta.env.DEV && <LogPanel />}
    </>
  );
}
```

- [ ] **Step 3: 跑 tsc 验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 4: 启动 dev 验证面板可显示（手动）**

Run: `cd D:/xjj20/Desktop/itv_2 && npm run dev`
操作：在浏览器按 Ctrl+Shift+L，面板应出现在右下角。再按一次隐藏。

- [ ] **Step 5: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/App.tsx
git commit -m "feat(logging): mount LogPanel in dev mode"
```

---

## Task 12: 扩展 `SettingsCategory` 加 `logging` 分类

**Files:**
- Modify: `src/features/settings/settingsEvents.ts`

- [ ] **Step 1: 在 `SettingsCategory` 联合类型中加 `'logging'`**

修改 `src/features/settings/settingsEvents.ts`：

```typescript
export type SettingsCategory =
  | 'imageApis'
  | 'pricing'
  | 'appearance'
  | 'general'
  | 'experimental'
  | 'logging';
```

- [ ] **Step 2: 跑 tsc 验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx tsc --noEmit`
Expected: SettingsDialog 中未覆盖的 case 会报 error，预期内（Task 13 修）。

- [ ] **Step 3: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/features/settings/settingsEvents.ts
git commit -m "feat(logging): add logging category to SettingsCategory type"
```

---

## Task 13: 在 `SettingsDialog.tsx` 加 logging 分类 UI

**Files:**
- Modify: `src/components/SettingsDialog.tsx`

- [ ] **Step 1: 在 `SettingsDialog` 文件顶部加 import**

```typescript
import { getLogConfig, setLogConfig, resetLogConfig, useLogStore } from '@/lib/logger';
import { openUrl } from '@tauri-apps/plugin-opener';
import { invoke } from '@tauri-apps/api/core';
```

> 注：项目使用 `@tauri-apps/plugin-opener` 还是别的，参照 `system.ts` 已有的 import 调整。

- [ ] **Step 2: 找到 settings 菜单的渲染处，在数组里加 'logging' 入口**

找一个类似下面的列表，加一项：

```tsx
{ key: 'logging', label: t('settings.categories.logging') ?? '日志' }
```

> 注：实际渲染方式以文件现有代码为准。可能用 `switch` 渲染对应面板，也可能用对象映射。Task 通过在 `switch` 末尾加 `case 'logging':` 完成。

- [ ] **Step 3: 找到主渲染的 `switch`/`if-else` 末尾，加 `case 'logging'` 分支**

```tsx
case 'logging':
  return <LoggingSettings />;
```

在文件底部加 `LoggingSettings` 组件：

```tsx
function LoggingSettings(): JSX.Element {
  const [config, setLocal] = useState(getLogConfig());
  const [moduleText, setModuleText] = useState(
    Object.entries(config.moduleLevels).map(([k, v]) => `${k}=${v}`).join(',')
  );
  const { t } = useTranslation();

  function commitModuleText(text: string) {
    setModuleText(text);
    const out: Record<string, 'debug' | 'info' | 'warn' | 'error'> = {};
    for (const part of text.split(',').map((s) => s.trim()).filter(Boolean)) {
      const [k, v] = part.split('=');
      if (k && (v === 'debug' || v === 'info' || v === 'warn' || v === 'error')) {
        out[k] = v;
      }
    }
    const next = setLogConfig({ moduleLevels: out });
    setLocal(next);
  }

  return (
    <div className="space-y-4">
      <h3 className="text-lg font-semibold">{t('logger.panel.title')}</h3>

      <div>
        <label className="text-sm">全局级别</label>
        <select
          className="ml-2 bg-zinc-800 px-2 py-1 rounded"
          value={config.level}
          onChange={(e) => {
            const next = setLogConfig({ level: e.target.value as any });
            setLocal(next);
          }}
        >
          <option value="debug">debug</option>
          <option value="info">info</option>
          <option value="warn">warn</option>
          <option value="error">error</option>
        </select>
      </div>

      <div>
        <label className="text-sm">模块级别覆盖</label>
        <input
          type="text"
          className="ml-2 bg-zinc-800 px-2 py-1 rounded w-96"
          value={moduleText}
          onChange={(e) => commitModuleText(e.target.value)}
          placeholder="canvas=debug,ai=warn"
        />
        <p className="text-xs text-zinc-500 mt-1">格式: 模块前缀=级别,逗号分隔。最长前缀优先匹配。</p>
      </div>

      <div className="flex gap-4">
        <label className="text-sm">
          <input
            type="checkbox"
            checked={config.console}
            onChange={(e) => setLocal(setLogConfig({ console: e.target.checked }))}
          />
          <span className="ml-2">控制台输出</span>
        </label>
        <label className="text-sm">
          <input
            type="checkbox"
            checked={config.persist}
            onChange={(e) => setLocal(setLogConfig({ persist: e.target.checked }))}
          />
          <span className="ml-2">持久化到文件</span>
        </label>
        <label className="text-sm">
          <input
            type="checkbox"
            checked={config.consoleTimestamps}
            onChange={(e) => setLocal(setLogConfig({ consoleTimestamps: e.target.checked }))}
          />
          <span className="ml-2">控制台时间戳</span>
        </label>
      </div>

      <div className="flex gap-2">
        <button
          className="px-3 py-1 bg-zinc-800 rounded text-sm"
          onClick={async () => {
            // 通过现有 system::open_log_dir 之类命令；若无则先 fallback
            try {
              await invoke('open_log_directory');
            } catch {
              // fallback: 提示用户
              alert('请通过 Storyboard Copilot 设置中的"打开日志目录"按钮');
            }
          }}
        >
          {t('logger.panel.openFolder')}
        </button>
        <button
          className="px-3 py-1 bg-zinc-800 rounded text-sm"
          onClick={() => {
            const entries = useLogStore.getState().snapshot().slice(-100);
            navigator.clipboard.writeText(
              entries.map((e) => `[${e.level}] ${e.target}: ${e.message}`).join('\n')
            );
          }}
        >
          {t('logger.panel.copyAll')}
        </button>
        <button
          className="px-3 py-1 bg-zinc-800 rounded text-sm"
          onClick={() => {
            resetLogConfig();
            setLocal(getLogConfig());
            setModuleText('');
          }}
        >
          重置
        </button>
      </div>
    </div>
  );
}
```

> 注：`open_log_directory` 命令需要新增。**改方案**：复用 `tauri-plugin-opener` 直接 `openPath()` 打开本地目录（`resolve_log_dir` 路径）。先在 `tauri-plugin-opener` 已有权限基础上加一个 `commands::logging::open_log_dir` 命令更干净。**作为本 Task 13 的"补做步骤"** 加该命令：

- [ ] **Step 4: 在 `src-tauri/src/commands/logging.rs` 加 `open_log_dir` 命令**

在文件末尾加：

```rust
#[tauri::command]
pub async fn open_log_dir(app: AppHandle) -> Result<(), String> {
    use tauri_plugin_opener::OpenerExt;
    let dir = crate::resolve_log_dir().ok_or_else(|| "no log dir".to_string())?;
    app.opener()
        .open_path(dir.to_string_lossy().to_string(), None::<&str>)
        .map_err(|e| e.to_string())?;
    Ok(())
}
```

> `resolve_log_dir` 是 `lib.rs` 里的私有函数，要么改成 `pub(crate)`，要么把命令放到 `lib.rs` 内。**建议**：把 `resolve_log_dir` 改 `pub(crate)`，然后在 `logging.rs` 用 `crate::resolve_log_dir()`。

- [ ] **Step 5: 在 `lib.rs` 的 `invoke_handler` 列表注册 `open_log_dir`**

加：

```rust
            logging::open_log_dir,
```

- [ ] **Step 6: 跑 `cargo check` 和 `npx tsc --noEmit`**

Run: `cd D:/xjj20/Desktop/itv_2 && npx tsc --noEmit && cd src-tauri && cargo check`
Expected: 全部通过。

- [ ] **Step 7: 启动 dev 手动验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npm run tauri dev`
操作：设置 → 日志分类 → 改级别/开关/打开目录；都生效。

- [ ] **Step 8: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/components/SettingsDialog.tsx src-tauri/src/commands/logging.rs src-tauri/src/lib.rs
git commit -m "feat(logging): add logging settings UI and open_log_dir command"
```

---

## Task 14: 批量替换 20 个文件中的 `console.*` 调用

**Files:**（修改）
- `src/App.tsx`
- `src/stores/canvasStore.ts`
- `src/stores/settingsStore.ts`
- `src/features/canvas/Canvas.tsx`
- `src/features/canvas/CanvasToolbar.tsx`
- `src/features/canvas/NodeSelectionMenu.tsx`
- `src/features/canvas/application/imageData.ts`
- `src/features/canvas/infrastructure/tauriAiGateway.ts`
- `src/features/canvas/infrastructure/textPolishService.ts`
- `src/features/canvas/ui/NodeActionToolbar.tsx`
- `src/features/canvas/ui/NodeToolDialog.tsx`
- `src/features/canvas/nodes/VideoGenNode.tsx`
- `src/features/canvas/nodes/VideoResultNode.tsx`
- `src/features/canvas/nodes/UploadNode.tsx`
- `src/features/canvas/nodes/SD2AudioUploadNode.tsx`
- `src/features/canvas/nodes/SD2VideoGenNode.tsx`
- `src/features/canvas/nodes/SD2VideoUploadNode.tsx`
- `src/features/canvas/nodes/StoryboardNode.tsx`
- `src/commands/ai.ts`
- `src/commands/image.ts` （同时删除 `logFrontendDebug` 函数）
- `src/commands/system.ts` （同时删除 `writeDebugLog` 函数及调用）
- `src/components/SettingsDialog.tsx`

- [ ] **Step 1: 在每个文件顶部加 import（按需）**

```typescript
import { logger } from '@/lib/logger';
```

> 路径别名以 `tsconfig.json` 为准。如果用相对路径：`from '../lib/logger'` 或 `from '../../lib/logger'` 等。

- [ ] **Step 2: 批量替换（脚本）**

创建一次性脚本 `scripts/console-to-logger.cjs`（**不入版本库**）：

```javascript
// scripts/console-to-logger.cjs
const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

const FILES = [
  // 上面 22 个文件，src 目录下
];

const LEVEL_MAP = {
  log: 'debug',
  info: 'info',
  warn: 'warn',
  error: 'error',
};

for (const f of FILES) {
  let src = fs.readFileSync(f, 'utf8');
  for (const [from, to] of Object.entries(LEVEL_MAP)) {
    const re = new RegExp(`console\\.${from}\\(`, 'g');
    src = src.replace(re, `logger.${to}(`);
  }
  fs.writeFileSync(f, src);
  console.log('replaced:', f);
}
```

然后：

```bash
cd D:/xjj20/Desktop/itv_2
node scripts/console-to-logger.cjs
rm scripts/console-to-logger.cjs
```

- [ ] **Step 3: 删除 `src/commands/image.ts` 中的 `logFrontendDebug` 函数**

打开文件，删除整个 `export async function logFrontendDebug(...)` 块。

- [ ] **Step 4: 删除 `src/commands/system.ts` 中的 `writeDebugLog` 函数及调用**

打开文件，删除 `writeDebugLog` 函数。然后 `grep -r "writeDebugLog" src` 找调用方，逐个删除或改为 `logger.warn(msg)`。

- [ ] **Step 5: 删除后端 `image::log_frontend_debug` 和 `system::write_debug_log`**

打开 `src-tauri/src/commands/image.rs`，删除 `pub async fn log_frontend_debug` 整个函数。

打开 `src-tauri/src/commands/system.rs`，删除 `pub fn write_debug_log` 整个函数。

- [ ] **Step 6: 在 `lib.rs` 的 `invoke_handler` 列表中删除两个旧命令**

```diff
-            image::log_frontend_debug,
             image::auto_save_video_to_project,
```

```diff
-            system::write_debug_log,
```

- [ ] **Step 7: 跑 tsc 和 cargo check**

Run: `cd D:/xjj20/Desktop/itv_2 && npx tsc --noEmit && cd src-tauri && cargo check`
Expected: 全部通过。

- [ ] **Step 8: 跑测试**

Run: `cd D:/xjj20/Desktop/itv_2 && npx vitest run`
Expected: 全部通过。

- [ ] **Step 9: 启动 dev，手动验证 `console.*` 替换没有引入回归**

Run: `cd D:/xjj20/Desktop/itv_2 && npm run tauri dev`
操作：触发已知会输出日志的代码路径（如生成图片），看：
- 浏览器 DevTools 有日志（来自新 logger 的 console 输出）
- `storyboard.log` 有 `frontend` target 的日志
- Ctrl+Shift+L 打开 LogPanel 能看到

- [ ] **Step 10: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add src/ src-tauri/src/
git commit -m "refactor(logging): replace 160 console.* calls with logger API and remove old debug commands"
```

---

## Task 15: 加 ESLint `no-console: warn` 规则

**Files:**
- Modify: `.eslintrc` 或 `eslint.config.js`（按项目实际配置格式）

- [ ] **Step 1: 找 ESLint 配置文件**

```bash
ls -la D:/xjj20/Desktop/itv_2/.eslint* D:/xjj20/Desktop/itv_2/eslint.config.*
```

按实际找到的文件修改。

- [ ] **Step 2: 在 rules 中加**

```json
{
  "rules": {
    "no-console": "warn"
  }
}
```

- [ ] **Step 3: 跑 eslint 验证**

Run: `cd D:/xjj20/Desktop/itv_2 && npx eslint src/ --ext .ts,.tsx 2>&1 | head -30`
Expected: 看到一堆 `no-console` warning（说明规则生效）；没 error 就好。

- [ ] **Step 4: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add .eslintrc
git commit -m "chore(logging): enable no-console warn rule"
```

---

## Task 16: 端到端冒烟测试

- [ ] **Step 1: 启动 Tauri dev**

Run: `cd D:/xjj20/Desktop/itv_2 && npm run tauri dev`

- [ ] **Step 2: 验证前端日志落 4 个地方**

操作：触发一处会产生日志的用户操作（如打开设置、生成图片、上传图片）。
验证：
- [ ] 浏览器 DevTools console 有日志（带 `[namespace]` 前缀）
- [ ] LogPanel（Ctrl+Shift+L）出现这条日志
- [ ] `storyboard.log` 文件（由 `resolve_log_dir()` 解析的路径）有这条日志
- [ ] 设置 → 日志 → 改全局级别为 `warn`，验证之前的 `info` 日志被过滤

- [ ] **Step 3: 验证降级**

操作：单独跑 `npm run dev`（不起 Tauri），打开浏览器。
验证：
- [ ] console 有日志
- [ ] LogPanel 有日志
- [ ] 没有报错（IPC 失败被静默）

- [ ] **Step 4: 验证快捷键不冲突**

操作：在输入框（textarea）按 Ctrl+Shift+L，验证不触发面板开关。

- [ ] **Step 5: 验证清空和复制**

操作：点 LogPanel "清空"，看到 "暂无日志"。点 "复制最近 100 条"，验证剪贴板有内容。

- [ ] **Step 6: 如果任何一步失败，添加 issue 不通过；全部通过则本 Task 完成**

- [ ] **Step 7: 提交（如有调整）**

```bash
cd D:/xjj20/Desktop/itv_2
git status  # 确认无变化或仅文档/微调
```

---

## Task 17: 写 CHANGELOG

**Files:**
- Create: `docs/releases/v0.1.14.md` （版本号按实际当前决定）

> 读 `package.json` 当前版本号，patch +1。CLAUDE.md 第 3.5 节有"推送更新"快捷口令流程，可在本 Task 完成后使用。本次仅写 changelog，发布由用户触发。

- [ ] **Step 1: 读 `package.json` 和 `src-tauri/Cargo.toml` 当前版本号**

```bash
cd D:/xjj20/Desktop/itv_2
grep '"version"' package.json
grep '^version' src-tauri/Cargo.toml
```

- [ ] **Step 2: 写 `docs/releases/v<next>.md`**

```markdown
## 新增

- 前端结构化日志系统：命名空间、级别、字段、运行时配置
- Dev-only 浮动日志面板（Ctrl+Shift+L 切换）
- 设置 → 日志分类：级别、模块覆盖、开关控制、打开目录、复制、清空
- 后端 Tauri command `append_frontend_log`，前后端日志同源持久化
- 启动时自动清理 14 天前的日志文件

## 优化

- 统一前后端日志时间线：前端日志通过后端 `tracing` 写入 `storyboard.log`
- 字段序列化保护：循环引用标记、大对象截断、Error 实例展开
- IPC 批量节流（200ms flush），避免高频日志淹没通道

## 修复

- 替换散乱的 160 处 `console.*` 调用为统一 logger
- 移除旧的 `log_frontend_debug` 和 `write_debug_log` 临时方案
```

- [ ] **Step 3: 提交**

```bash
cd D:/xjj20/Desktop/itv_2
git add docs/releases/
git commit -m "docs(release): add changelog for v<next>"
```

---

## 自审（实施前最后一步）

- [ ] **Spec 覆盖检查**：
  - §3 前端 Logger API → Task 4, 9
  - §4 后端 command → Task 2
  - §5 持久化与轮转 → Task 3, 4（默认 config）
  - §6 运行时配置 → Task 7, 13
  - §7 浮动面板 → Task 10, 11
  - §8 重构 console.* → Task 14
  - §9 错误处理与降级 → Task 8（降级）+ Task 4（默认配置）
  - §10 测试 → Task 5/6/7/8 (单测) + Task 16 (E2E 冒烟)

- [ ] **占位符检查**：✅ 无 TBD / TODO / FIXME / "implement later"

- [ ] **类型一致性**：
  - `LogEntry` 字段在 types.ts（Task 4）定义，transport.ts（Task 8）使用 `LogEntry` 导入，index.ts（Task 9）构造同名对象 ✅
  - `LogConfig` 字段在 types.ts 定义，store/transport/config 都引用 ✅
  - `Level` 联合类型在所有地方一致 ✅
  - `Level = 'debug' | 'info' | 'warn' | 'error'` ✅

- [ ] **可能的小调整**：
  - `namespace.ts` 的 `resolveNamespace(depth)` 深度参数需要按调用栈层级调整；如果实测不准确，可能要在 Task 11 联调时调整 stack 深度值。
  - Task 13 的 `open_log_dir` 借用 `tauri-plugin-opener` 的 `openPath` API；如果该 API 签名不对应实际版本，需要回退到 `Command::new("explorer").arg(dir).spawn()` 之类。

---

## 执行选项

Plan 完成后，**两个执行方式**：

1. **Subagent-Driven**（推荐）：每 task 分派一个新子代理，task 之间做 code review，迭代快
2. **Inline Execution**：在当前会话用 `executing-plans` 批量执行，checkpoint 检查

你选哪个？
